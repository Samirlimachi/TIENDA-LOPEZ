// IP local de la PC donde corre el backend (ver `npm run dev` en /backend).
// El celular debe estar conectado a la misma red WiFi que la PC para desarrollo.
export const API_BASE_URL = 'http://192.168.100.40:4000';
